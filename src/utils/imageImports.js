// Import all images from assets folder
// This file centralizes image imports to make them easier to manage

// Example imports - replace with your actual image imports
import logo from '../assets/logo.png';
import hero from '../assets/hero.jpg';
// Add all your other image imports here

// Export as an object for easy access
const images = {
  logo,
  hero,
  // Add all your other images here
};

export default images;
